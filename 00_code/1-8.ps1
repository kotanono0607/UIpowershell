function 1_8 {
    # ダイアログを表示して値を取得
    $入力された数値 = 数値を入力 -フォームタイトル "数値の入力" -ラベルテキスト "入力したい数値を入力してください:"

    # 選択された値を使って固定値のコードを生成
    if ($入力された数値 -ne $null) {
        return "指定秒待機 -秒数 $入力された数値"
    } else {
        return "# キャンセルされました"
    }
}